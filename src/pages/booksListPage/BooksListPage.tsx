import React from 'react';

import { BooksList } from 'components';
import { ReturnComponentType } from 'types';

export const BooksListPage = (): ReturnComponentType => {
    return <BooksList />;
};
